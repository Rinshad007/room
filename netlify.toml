[build]
  base    = "budget-buddy/frontend"
  command = "npm run build"
  publish = "budget-buddy/frontend/dist"

[build.environment]
  NODE_VERSION = "20"

# SPA redirect — all routes serve index.html so React Router works
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
